#pragma once

#include <image/CoreTypes.hpp>
#include <image/data/Declare.hpp>

namespace image {

    TYPE(bool, {});
    TYPE(I32, {});
    TYPE(F32, {});
    TYPE(String, {});
    TYPE(Path, {});

}
